(ns eita2
  (:require
   [jank.export :as je]))

(cpp/raw "
int eita2(int a, int b) {
   return a + b;
}
")

(defn simple
  []
  (cpp/eita2 30 5))

(jank.export/register
 #'simple
 "simple"
 [:=> [:cat] :int])

(defn -main [& _args]
  #_(simple)
  (println 444))
